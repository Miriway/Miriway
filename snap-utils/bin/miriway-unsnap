#! /bin/bash
set -e

unset __EGL_VENDOR_LIBRARY_DIRS
unset LIBGL_DRIVERS_PATH
unset LIBINPUT_QUIRKS_DIR

shopt -s extglob
export PATH="${PATH##*($SNAP*([^:])?:)}"
export LD_LIBRARY_PATH="${LD_LIBRARY_PATH##*(/var/lib/snapd/*([^:])?:)*($SNAP*([^:])?:)*($SNAP*([^:]))}"
exec "$@"